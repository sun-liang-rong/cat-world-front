# 性能优化方案

## 已识别的性能瓶颈

### 1. 动画和补间（Tween）
- GameScreen.ts 有 2819 行代码，包含大量动画
- 78 处使用 tween/forEach/setTimeout
- 开场动画、骰子洗牌、卡片飞入等都可能在低端机卡顿

### 2. 节点管理
- 大量动态创建和销毁节点
- 每次刷新可能重建整个节点树
- 缺少对象池复用机制

### 3. 图形绘制
- 使用 Graphics 组件动态绘制面板、按钮
- 每帧可能触发重绘

## 优化措施

### A. 动画优化（已实施部分）

#### 1. 减少开场动画的批次间隔
- 原来：每批等待 28ms
- 建议：根据设备性能动态调整

#### 2. 控制同时运行的补间数量
- 限制同屏最多运行的动画数量
- 低端设备降低动画复杂度

#### 3. 使用缓动函数优化
- 简单动画用 `linear`、`sineOut` 等轻量缓动
- 避免复杂的 `elastic`、`bounce` 缓动

### B. 渲染优化

#### 1. 对象池（Object Pooling）
为频繁创建销毁的节点建立对象池：
- 卡片节点（Tile）
- Toast 提示
- 特效节点

#### 2. 静态缓存
- 将不变的 Graphics 绘制结果缓存为纹理
- 预加载所有资源，避免运行时加载

#### 3. 批量更新
- 减少单帧内的节点操作
- 合并多次 setPosition/setScale 调用

### C. 代码层面优化

#### 1. 减少 forEach 嵌套
- 用 for 循环替代 forEach（性能更好）
- 避免在循环内创建闭包

#### 2. 事件监听优化
- 及时移除不用的事件监听
- 使用事件委托减少监听器数量

#### 3. 定时器管理
- 统一管理所有 setTimeout/setInterval
- 页面销毁时清理所有定时器

### D. 低端设备适配

#### 1. 性能分级
```typescript
// 根据设备性能调整
const deviceTier = this.detectDeviceTier();
if (deviceTier === 'low') {
  // 降低动画质量
  // 减少粒子效果
  // 简化阴影和模糊
}
```

#### 2. 帧率控制
- 低端设备锁定 30fps
- 中高端设备 60fps

#### 3. 动画降级策略
低端设备：
- 跳过非关键动画
- 减少补间时间
- 降低粒子数量
- 禁用复杂特效

## 具体实施建议

### 立即可做（影响最大）

1. **添加对象池**：为 Tile 节点建立复用池
2. **优化开场动画**：减少同时播放的动画数量
3. **批量更新**：合并 sortBoardTileNodes() 等批量操作
4. **清理定时器**：确保所有 timer 都被正确清理

### 中期优化

1. **纹理缓存**：将 Graphics 绘制转换为精灵图
2. **LOD 系统**：根据设备性能动态调整质量
3. **懒加载**：延迟加载非关键资源

### 长期优化

1. **WebWorker**：将复杂计算移到后台线程
2. **资源压缩**：优化图片大小和格式
3. **代码分割**：按需加载模块

## 监控指标

- FPS（帧率）
- 内存占用
- 节点数量
- 补间数量
- 加载时间

## 测试设备

建议在以下设备测试：
- 低端：微信小游戏基础库 2.0（Android 低端机）
- 中端：iPhone 8 / 小米 9
- 高端：iPhone 13+ / 旗舰 Android

